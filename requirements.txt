anthropic>=0.40.0
python-dotenv>=1.0.0
# spx_inclusion_momentum.py uses only stdlib (math, random, statistics, dataclasses, datetime)
# telegram/handler.py uses only stdlib (json, urllib, os, sys, datetime)
# No external data dependencies — dataset is embedded in spx_inclusion_momentum.py
